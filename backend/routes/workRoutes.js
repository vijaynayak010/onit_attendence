import express from 'express';

const router = express.Router();

// All work-related functionalities are currently handled within employeeRoutes and adminRoutes 
// to adhere strictly to the role-based path requirements (e.g., /api/employee/add-work, /api/admin/work-updates)
// This file serves as a placeholder for future generic/public work routes if needed.

export default router;
